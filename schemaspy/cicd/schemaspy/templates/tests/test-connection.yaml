apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "schemaspy.fullname" . }}-test-connection"
  labels:
    {{- include "schemaspy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "schemaspy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
