package specs

import specs.traits.Login

/**
 * Base spec for tests that require login
 */
abstract class LoggedInSpec extends BaseSpec implements Login {
  // initial placeholder, maybe add cleanup?
}